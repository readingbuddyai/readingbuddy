# Backend API 서버 URL
# Docker에서는 host.docker.internal 사용 (Windows/Mac)
# Linux에서는 host IP 또는 서비스명 사용
BASE_URL=http://host.docker.internal:8080

# 자동 테스트 설정
NUM_USERS=20
MAX_WORKERS=10
STAGE=4.1
PROBLEM_COUNT=5
AUTO_SIGNUP=true
