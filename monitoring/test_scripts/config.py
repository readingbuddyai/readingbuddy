# ============================================================
# 백엔드 서버 설정
# ============================================================
# Spring Boot 백엔드가 실행되는 URL
BASE_URL = "http://localhost:8080"

# ============================================================
# 테스트 계정 정보
# ============================================================
# 실제 회원가입한 계정 정보를 입력하세요
# 이 계정으로 로그인하여 JWT 토큰을 받아옵니다
TEST_USER = {
    "email": "user@example.com",        # 실제 이메일 주소 입력
    "password": "password!@123"      # 실제 비밀번호 입력
}

# ============================================================
# 테스트 설정
# ============================================================
TEST_CONFIG = {
    # 훈련 4.1에서 생성할 문제 개수
    "stage_4_1_count": 5,

    # 훈련 4.2에서 생성할 문제 개수
    "stage_4_2_count": 5,

    # 부하 테스트 시 API를 반복 호출할 횟수
    # 예: 10이면 총 10번 API를 호출하여 성능 테스트
    "load_test_iterations": 10,

    # 각 요청 사이의 대기 시간 (초)
    # 서버에 과부하를 주지 않기 위해 적절한 딜레이 설정
    "delay_between_requests": 1,
}

# ============================================================
# Prometheus 메트릭 서버 설정
# ============================================================
# 테스트 스크립트가 메트릭을 노출할 포트 번호
# Prometheus가 이 포트로 메트릭을 수집합니다
METRICS_PORT = 8000

