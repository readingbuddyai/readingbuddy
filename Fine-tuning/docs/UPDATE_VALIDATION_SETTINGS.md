# 🚀 Validation 최적화 가이드

## 📊 **현재 문제**

### GPU 1 (r=16, 진행 중)
```
eval_steps=500        # 500 스텝마다 평가 (너무 자주!)
Validation: 114,539개 (전체) # 너무 많음!
평가 시간: 14.5분/회
총 평가 시간: 6.7일 (전체 학습의 40%)
```

### GPU 2 (r=32, 새로 시작)
```
eval_steps=2000       # 2000 스텝마다 평가 ✅
Validation: 20,000개  # 최적화 ✅
평가 시간: 2.5분/회
총 평가 시간: 0.3일 (전체 학습의 3%)
```

---

## ✅ **GPU 2: r=32 시작하기**

### 실행 명령어
```bash
cd /home/j-k13a206/fine_tunining_new

# 백그라운드 실행
nohup python3 train_all_phases_all_r32_optimized.py \
  --phase 1 \
  --gpu 2 > training_r32_optimized.log 2>&1 &

# 프로세스 ID 확인
echo $!

# 로그 확인
tail -f training_r32_optimized.log
```

### 예상 결과
```
학습 시간: 10-11일 (vs r=16의 16일)
예상 PER: 9-11% ⭐
평가 시간: 전체의 3%만 차지
```

---

## ⚠️ **GPU 1: 현재 학습 어떻게 할까?**

### 옵션 1: 그대로 두기 (추천)
```
장점:
  ✅ 5000 steps 이미 진행 (1.5%)
  ✅ 중단하면 시간 낭비
  ✅ r=16도 충분히 좋은 성능

단점:
  ⚠️ 16일 걸림 (vs 10일)
  ⚠️ 평가 시간 낭비
```

### 옵션 2: 중단하고 최적화 버전으로 재시작
```
장점:
  ✅ 10일로 단축
  ✅ 평가 시간 절약

단점:
  ⚠️ 5000 steps 버림 (1일 분량)
  ⚠️ 처음부터 다시 시작
```

---

## 💡 **추천: 옵션 1 (그대로 두기)**

### 이유
```
1. 이미 1.5% 진행 (5000 steps)
2. r=16, r=32 병렬로 비교 가능
3. 리스크 분산
4. 추가 6일은 acceptable
```

### 10일 후 결과
```
GPU 1 (r=16): 16일 소요, PER 10-12%
GPU 2 (r=32): 10일 소요, PER 9-11% ⭐

→ GPU 2를 선택하면 됨!
```

---

## 📊 **비교표**

| 항목 | GPU 1 (r=16, 현재) | GPU 2 (r=32, 최적화) |
|------|-------------------|---------------------|
| **LoRA Rank** | 16 | 32 ⭐ |
| **Eval Steps** | 500 | 2000 ⭐ |
| **Val 샘플** | 114,539 | 20,000 ⭐ |
| **평가 시간** | 14.5분 | 2.5분 ⭐ |
| **총 학습 시간** | 16일 | 10일 ⭐ |
| **예상 PER** | 10-12% | 9-11% ⭐ |

---

## 🎯 **실행 계획**

### 지금 바로
```bash
# GPU 2 시작
nohup python3 train_all_phases_all_r32_optimized.py \
  --phase 1 \
  --gpu 2 > training_r32_optimized.log 2>&1 &
```

### 모니터링
```bash
# GPU 1 (r=16)
tail -f training_full_3500h.log

# GPU 2 (r=32)
tail -f training_r32_optimized.log

# GPU 사용률
watch -n 5 nvidia-smi
```

### 10일 후
```bash
# 두 모델 비교
python3 evaluate_test.py --model checkpoints_full/phase1_full_3500h/final_model --gpu 0
python3 evaluate_test.py --model checkpoints_full_r32/phase1_full_3500h_r32/final_model --gpu 0

# 더 좋은 모델 선택!
```

---

## 📝 **정리**

1. **GPU 1**: 그대로 두기 (16일 걸리지만 OK)
2. **GPU 2**: r=32 최적화 버전 시작 (10일, 최고 성능)
3. **10일 후**: 둘 중 더 좋은 모델 선택

**결과**: 어떤 경우든 성공! 🎉
